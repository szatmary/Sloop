// See ../README.md — kept outside libtailscale.xcframework because Xcode
// merges every xcframework's Headers into one include/ directory, where a
// second module.modulemap collides with libssh2's.
module CTailscale {
    header "../../libtailscale.xcframework/ios-arm64-simulator/Headers/tailscale.h"
    export *
}
